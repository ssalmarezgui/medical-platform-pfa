input {
  tcp {
    port => 5044
    codec => json_lines
  }
}

output {
  elasticsearch {
    hosts => ["elasticsearch:9200"]
    index => "nephrocare-logs-%{+YYYY.MM.dd}"
  }
  # Optionnel : affiche également les logs dans la console de Docker
  stdout { codec => rubydebug }
}